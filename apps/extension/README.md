# SceneArc Chrome Extension (Phase Two)

This directory is intentionally minimal during Phase One.

The companion Chrome extension — a Manifest V3 side-panel app that pairs with the
SceneArc web app, inserts prepared prompts, and imports approved results — is
built in **Phase Two**. See `PROJECT_PLAN.md` for the full Phase Two scope.

Nothing here is wired into the build yet.
