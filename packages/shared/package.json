{
  "name": "@scenearc/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and zod schemas for SceneArc. Contains no secrets and no proprietary prompt text.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@scenearc/config": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
